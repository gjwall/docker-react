language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -f Dockerfile.dev -t r3dshift/docker-react .

script:
  - docker run -e CI=true r3dshift/docker-react npm run test